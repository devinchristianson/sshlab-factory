groups:
  admin:
    members:
      - devinchristianson
    environments:
      - "*"
    overrides: {}
  test:
    members:
      - joe
    environments:
      - test
    overrides:
environments:
  test: {}
  k3s:
    docker:
      execution:
        container:
          image: devinchristianson/k3dlab:latest
        host:
          binds:
            - "/var/run/docker.sock:/var/run/docker.sock"